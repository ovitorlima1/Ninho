# Copy to .env for local development: cp .env.example .env

# Postgres from docker-compose.dev.yml
DATABASE_URL=postgres://ninho:ninho@localhost:5460/ninho

# At least 32 characters. Generate one with: openssl rand -hex 32
SESSION_SECRET=

# Local ports (8080 and 5173 are often taken by other projects)
API_PORT=8787
WEB_PORT=5180

LOG_LEVEL=info

# Password recovery (not functional locally: e-mail is sent through Replit
# Connectors). In production, PUBLIC_APP_URL must be an HTTPS URL.
# PUBLIC_APP_URL=https://ninho.example.com
# RESEND_FROM_EMAIL=Ninho <no-reply@example.com>
